The file that executes the attack is client .
The make file will compile the code .
The function execute_command will execute the command that is in string format given as argument in the terminal 
The function insert_ssh will insert your public ssh key in the Virtual Machine , the SSH key must be put at the 13th line : #define SSH
You will find two screenshots , the first is the result of me inserting my name in the website , the second is where i succeeded to connect with ssh commabd to the virtual machine.